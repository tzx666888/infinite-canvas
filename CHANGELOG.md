# CHANGELOG

## Unreleased

## v3.0.62 - 2026-08-06

+ [修正] “一组不同场景电商详情图”和“不同场景详情图”统一按批量详情套图处理，继续尊重用户选择的生成数量。
+ [版式] 每张结果使用独立场景与自由详情页构图，可采用主视觉、分栏、非对称、编辑式或多区块布局，不固定为 2×2 或任何宫格数量。
+ [保护] 仅用户明确要求 2×2、九宫格、拼图等布局时才锁定多面板；普通换风格仍保持单画面。本版未修改图片模型、价格、尺寸、并发或视频链路。

## v3.0.61 - 2026-08-06

+ [修复] 从图片节点直接生成“一组/一套/不同场景电商详情图”时，参考图改为产品身份依据，不再误套“保留原构图”的普通编辑规则。
+ [统一] 同一详情套图提示无论从图片节点还是生成配置节点进入，都会编译为独立的 2×2 完整详情版式；批量结果按序使用不同场景方向。
+ [保护] 普通“不同风格、换背景、改颜色”仍保持单幅连续画面，不恢复无意生成宫格的旧问题；本版未修改视频模型、模板、路由或计费。

## v3.0.60 - 2026-08-06

+ [修复] 中转站补齐画布使用的 `/v1/videos/generations` 创建与查询兼容路由，Seedance 任务不再因单复数路径不一致而停在画布外。
+ [验收] `Seedance 2.0-fast-720p`、`qy-seedance-2.0` 和 `qy-seedance-2.0-fast` 均通过画布代理完成真实任务创建、轮询、受保护结果下载与播放文件校验。
+ [隔离] 三款 Seedance 继续按各自能力提交时长、分辨率、比例、声音及参考素材参数；本版未修改视频提示词和内置模板。

## v3.0.59 - 2026-08-06

+ [修复] 画布节点不再展示内部服务名、项目 ID、上传路径或原始上游报错；客户只会看到审核、额度、模型资源、超时或参考素材等可理解原因。
+ [修复] Veo 多参考按当前可用能力固定为原生 8 秒 720p，不再把不可用的 1080p 配置提交给上游。
+ [兼容] 三款 Seedance 统一只提交供应商实际接受的 5/10/15 秒，参考图片上限同步为 12MB。
+ [测试] 覆盖客户错误脱敏、Veo 多参考分辨率和 Seedance 参数归一回归。

## v3.0.58 - 2026-08-05

+ [新增] 画布 Agent 带货视频开放印度尼西亚、泰国、越南与中国市场；美国、英国、新加坡及日本、韩国、沙特、巴西、墨西哥按后续优先级保留禁用占位。
+ [本地化] 中国市场使用抖音 / 快手及真实中国生活场景，拦截 TikTok、促销词和广告法绝对化用语；泰文与中文口播分别按对应字符数校验。
+ [溯源] 菲律宾、马来西亚五份鑫哥原始语料保持逐字不变；四份新增语料明确标为 `derived-pending-review`，不混入原始语料档案。
+ [界面] 视频选项卡按 P0 → P1 → P2 展示市场，并随选择显示对应平台与语言语境；默认市场仍为菲律宾。
+ [新增] 画布 Agent 视频卡增加默认关闭的画面字幕开关；编译器和本地校验器只允许“纯净画面 / 逐镜字幕”两条规则中与开关匹配的一条。
+ [新增] 支持“先看提示词”后编辑镜头、动作、转场、音效和口播；参考图规则、达人首行、字幕规则和产品一致性尾句锁定，“用这段生成”不再二次编译。
+ [溯源] 人工改写视频提示词按 `user_typed` 记录，保留原 `templateId` 及可用于 `prompt_edited` 的编译前后文本对；参数变更会立即使缓存编辑稿失效。
+ [模型] Agent 视频卡从已配置且已接入共享视频 provider 的模型动态生成，开放 Omni、Veo 智能生成、Veo 多参考与固定 Seedance，排除两个 qy Seedance；横屏 Omni 真实路由为 `omni` / `1280x720`。
+ [测试] 新增六个 P0 市场编译、脚本长度、促销与中国合规回归，并通过 TypeScript、视频供应商、视频代理、视频工作台、12 宫格分镜及画布提示词回归。

## v3.0.57 - 2026-08-05

+ [新增] 画布 Agent 增加带货视频选项卡片，支持“纯手部实测 / 达人多场景”、菲律宾 / 马来西亚、Omni / Veo / Seedance 与横竖屏选择；达人模式可从画布选择或上传第二张达人参考图。
+ [接入] 卡片通过既有视频生成管线创建并回填视频节点；默认使用 Omni 竖屏，Seedance 保留可选并明确标注无声，印度尼西亚与泰国继续禁用。
+ [修复] M1 提示词长度目标改为非阻断 warning，仅超过 2400 字符硬失败；带货视频不再受“视频提示词只用英文”规则限制。
+ [修复] 15 秒达人 SOP 的每镜标签固定按“时长 → 转场手法 → ASMR 音效”顺序前置，校验与编译格式保持一致。
+ [安全] 一次只接收一张产品参考图，达人引用固定为“达人 → 产品”；模型失败仅提示用户手动换模型重试，不自动重复扣费。
+ [待验] Seedance 真实生成待渠道层修复后验，不阻塞本版 Agent 卡片交付。

## v3.0.56 - 2026-08-05

+ [调整] Google 静态生图只保留 `gemini-3.1-flash-image-4k`，模型同步、设置接口、下拉选项和视频首帧备用链路不再公开 1K/2K。
+ [兼容] 旧浏览器配置和历史画布节点中的无后缀、1K、2K ID 会在提交前统一迁移为 4K，不会掉回其他模型或继续请求已下架 ID。
+ [约束] Google 生图请求固定发送 `image_size=4K`，同时保留官方 14 种宽高比与对应原生 4K 像素。
+ [测试] 新增旧模型 ID 迁移和请求体强制 4K 回归，发布后真实生成 1:1 原图并核验 4096×4096 像素。

## v3.0.55 - 2026-08-04

+ [新增] 画布 Agent 增加独立视频 SOP 编译器，支持“纯手部实测”和“达人多场景”两套带货预设，不经过公共视频提示词优化链路。
+ [新增] 完整归档鑫哥 5 套原始 SOP，开放菲律宾与马来西亚市场语料，印度尼西亚与泰国保持禁用占位。
+ [约束] 本地校验覆盖参考图数量和作用域、镜头数和时间窗、当地语言口播、促销词、末镜 CTA、产品一致性尾句及正文长度；失败最多自动纠正一次。
+ [兼容] 视频模型时长、分辨率与比例继续复用现有归一链路：Seedance 15 秒，Veo 8 秒，Omni 10 秒。
+ [隔离] 本版只新增 Agent 专用预设和编译器，未修改公共画布生成、12 宫格、关键帧、视频创作台或现有提示词内容。

## v3.0.54 - 2026-08-04

+ [新增] 画布 Agent 的指定节点生成工具支持原样下发 `mode: "video"`，不再被 Agent 参数层改写为图片生成。
+ [安全] Agent 触发视频生成时始终弹出工具确认卡片，即使已关闭普通工具确认，也不会无声扣费。
+ [优化] Agent 工具循环上限由 8 步提升为 12 步，支持读取画布、确认、触发生成和结果回报的完整视频链路。
+ [隔离] 本版仅修改 Agent 面板，未更改公共画布生成、12 宫格、关键帧或视频创作台链路。

## v3.0.53 - 2026-08-04

+ [修复] 按固定 Seedance、通用 Seedance、Veo/Omni、Google 生图、GPT Image 2 与 TTS 分别重建请求白名单，不再携带其他模型的残留参数。
+ [修复] `Seedance 2.0-fast-720p` 完全移除不支持的 `generate_audio`、视频参考与音频参考字段；中转站 AIYM 适配器改为解析后重建上游 JSON，丢弃别名和未知字段。
+ [修复] `tts-1` 不再发送仅 GPT-4o TTS 支持的 `instructions`；Google 生图不再发送 GPT 图像质量参数。
+ [安全] 未知 `/v1/videos/generations` 模型立即返回真实错误，不再隐式改写为 Grok；移除令牌实际不可用的 Grok 生图兜底。
+ [测试] 新增跨模型参数污染回归，并通过 TypeScript、Next 生产构建与 NewAPI 任务适配器 Go 测试。

## v3.0.52 - 2026-08-04

+ [修复] GPT Image 2 自动兼容旧画布保存的 `standard` / `hd` 质量值，提交前分别归一为 `low` / `high`，避免上游以参数不合法拒绝生成。
+ [测试] 完成 GPT Image 2、Omni、Veo 3.1 与 Seedance 2.0 的线上真实链路验收，覆盖任务提交、轮询、结果下载、媒体规格与可播放性。

## v3.0.51 - 2026-08-04

+ [新增] 提示词中心加入 7 个 Tokaxis 可视化创作入口，覆盖商业脚本、真人商业摄影、产品视觉、静态图像、分镜情绪与通用视频提示词。
+ [优化] 提示词中心只保留商品电商、商业人物和 Tokaxis 创作内容，移除游戏动漫、界面设计、信息图、建筑等无关模块。
+ [优化] 图片、视频、批量与画布 Agent 的提示词选择器按创作媒介和工作流准确分流，工作流可直接交给 Agent 执行。

## v3.0.50 - 2026-08-03

+ [调整] Veo 3.1 取消 15 秒续写入口，文字、首帧、首尾帧和多参考统一使用原生 8 秒 1080p，旧节点保存的 15 秒也会在提交前归一为 8 秒。
+ [新增] 画布接入 `Seedance 2.0-fast-720p`、`qy-seedance-2.0` 和 `qy-seedance-2.0-fast`，按型号分别限制分辨率、比例、声音及图/视频/音频参考能力。
+ [修复] Seedance 改走中转站 `/v1/videos/generations` 异步协议，提交不再被 Grok 兼容层改写，轮询支持 `completed` 与 `video.url` 返回格式。
+ [修复] Seedance 成功后通过带令牌的画布代理下载受保护视频并持久化，避免中转站任务成功但画布没有可播放结果。
+ [测试] 新增 Seedance 能力矩阵、代理隔离、轮询回显与 Veo 固定 8 秒回归检查。

## v3.0.49 - 2026-08-02

+ [修复] 整片分镜视频改为结构化 Veo 执行合同，完整保留每个 beat 的景别、运镜、动作对象、场景与收尾状态，不再把分镜压缩成“toward / hand”等残句。
+ [修复] 首帧只锁定第一镜的人物、服装和商品身份，不再锁死后续构图、机位与计划场景；整片必须按 2/3/4 个编号镜头逐段执行。
+ [修复] Veo 15 秒官方续写按第 8 秒精确拆分镜头与口播；后半段移除首帧重启和“执行全部四镜”的冲突指令，不再重演开场。
+ [修复] 关键帧同时保存真实 beat 编号与序列位置；中间帧失败不再导致后续视频错配 beat，后续帧也不会被回填成伪首帧，多次生成时只取最近一批关键帧。
+ [优化] 12 宫格明确定义为执行 beat 的构图候选，不得自行增加行走、道具、服装或地点；新规划统一为 4/6 秒 2 镜、8/10 秒 3 镜、12/15 秒 4 镜。
+ [优化] 口播计划补全 4 秒与 8 秒独立文案，15 秒文案要求在约 8 秒处有自然分句，保证续写不重复、不机械截断。
+ [测试] 新增珠宝带货分镜、8 秒原生执行、15 秒新旧提示词拆分、跨 8 秒动作及关键帧失败/错位回归检查。

## v3.0.48 - 2026-08-02

+ [优化] 切换到 Veo 3.1 智能生成时默认选中 8 秒原生模式和真实 1080p，不再沿用上一模型的 15 秒配置。
+ [优化] 画布与视频创作台的时长按钮直接显示“4/6/8s · 1080p”和“15s续写 · 720p”，原生短片与官方续写不再混淆。
+ [测试] 新增 Veo、多参考和 Omni 入口默认时长/分辨率回归检查。

## v3.0.47 - 2026-08-02

+ [修复] 撤销 Veo 16 秒双任务伪接力，改为 Google 官方 Extend：先生成 8 秒基础片，再在同一账号、同一项目和原始媒体 ID 上续写 7 秒，返回 15 秒完整视频。
+ [修复] 续写阶段只接收剩余动作和后半段口播，不再带入“生成整条 15/16 秒”的矛盾指令，禁止重念开场和重启动作。
+ [容错] 基础片成功后会立即持久化；续写遇到临时超时时只在原任务上重试一次，安全审查、权限或参数错误立即返回真实原因。
+ [约束] 15 秒官方续写按上游真实能力固定 720p，仅支持一张首帧；4/6/8 秒原生模式继续使用真实 1080p 放大任务。
+ [测试] 新增账号锁定、官方续写编排、口播/时间线拆分、不可重试错误及 720p/1080p 分流回归。

## v3.0.46 - 2026-08-02

+ [新增] Veo 智能生成增加明确标注的“16 秒接力测试（2×8 秒）”：第一段完成后提取真实尾帧，作为第二段精确首帧，最终输出一条 16 秒视频。
+ [优化] 使用两张图时，第一张作为整条视频起始帧，第二张只作为第二段最终目标帧；两段口播自动拆分，第二段禁止重念开场或重新开始动作。
+ [约束] 16 秒必须由用户明确点选，旧的 12/15 秒配置不会自动升级为双任务；两段各只提交一次，任一步失败立即返回真实原因。
+ [测试] 增加 Veo/Omni/R2V 时长隔离、接力口播拆分及“首图 → 实际尾帧＋目标图”异步编排回归。

## v3.0.45 - 2026-08-02

+ [修复] Veo 3.1 按官方能力开放原生 4/6/8 秒，首尾帧模式固定为官方 8 秒；不再把两段 8 秒拼接伪装成 15 秒。
+ [修复] Veo 统一请求真实 1080p 上采样结果，并校验成片实际尺寸；上游未返回 1080p 时直接报错，不再把 720p 静默标成成功。
+ [约束] Omni 继续固定 10 秒 720p，本版未改动其提示词、分镜或首尾帧衔接逻辑。
+ [测试] 真实任务验收通过：Veo 文生视频输出 1920x1080、8.000 秒、含 AAC 音频；逐秒检查右下角未发现可见 Veo 水印。

## v3.0.44 - 2026-08-02

+ [优化] 视频模型选择从 9 个底层 ID 收口为“Veo 3.1 智能生成”“Veo 3.1 多参考”“Omni 智能创作”3 个能力入口；横竖屏继续由尺寸参数自动适配。
+ [修复] Google 视频统一使用固定路由矩阵：智能生成按 0/1/2 张图分别选择文生、首帧、首尾帧，3 张图立即提示切换多参考；多参考和 Omni 严格校验各自素材数量。
+ [优化] 视频创作台与画布配置节点显示本次实际路由、方向、分辨率和时长，避免静默切换底层模型。
+ [重构] Google、Grok 模型规则与 Google Flow、Seedance 请求协议按厂商拆分；公共视频服务只负责统一调度，后续修复不会跨厂商串改。
+ [测试] 增加三入口、横竖屏和参考图数量的路由回归；继续仅开放已验证的 720p 路径，1080p 待单独完成定价与真实任务验证后启用。

## v3.0.43 - 2026-07-31

+ [修复] 按 Flow2API 真实协议恢复 `omni` / `omni_portrait` 的参考图视频能力；无图走 T2V，连接 1–3 张图时走 Omni Reference Images 链路，固定 10 秒与 720p 不变。
+ [修复] 画布、视频创作台和 Agent 不再把 Omni 误报为纯文生模型；参考图数量、R2V 提示词编译与模型说明保持一致。
+ [测试] 新增 Omni 0–3 图能力、4 图拒绝和 T2V/R2V 模式回归，并通过桥接单元测试、TypeScript 与生产构建。

## v3.0.42 - 2026-07-31

+ [新增] 画布接入 Google/Flow 的 9 个视频模型，包含 Veo 3.1 文生、首尾帧、多参考的横竖屏模型以及 `omni` / `omni_portrait`；Grok 视频暂不在画布候选中显示。
+ [修复] Google 视频改用 multipart `/v1/videos` 异步任务协议，参考图作为真实文件上传，并通过轮询和鉴权内容接口回填可播放结果。
+ [约束] Veo 文生不接图、首尾帧支持 1–2 图、多参考支持 1–3 图且支持 4/6/15 秒；Omni 只支持纯文字并固定 10 秒，非法组合在提交阶段立即返回真实原因。
+ [测试] 新增 9 模型发现、参考模式、时长、multipart 协议与 Veo 分镜编译回归，并通过 TypeScript 和生产构建。

## v3.0.41 - 2026-07-31

+ [修复] 将 2 路保护收窄到参考图准备和任务提交阶段；服务端返回 `202` 后立即释放名额，画布选择 10 张时可全部进入后台并发生成。
+ [修复] 画布和生图工作台恢复最多 10 个任务并发，图片批量工作台恢复 1–5 路可调；批量停止或超时时同步取消持久图片任务。
+ [测试] 新增“上传最多 2 路、6 个后台任务无需等待前序生成即可全部进入轮询”的并发边界回归。

## v3.0.40 - 2026-07-31

+ [修复] 所有文生图和参考图编辑统一经过最多 2 路的可取消请求队列，包含 Google 多图内部请求；画布顶部“一键重试”不再同时提交最多 6 个失败节点。
+ [修复] 生图工作台改为 2 路有序任务池并启用持久图片任务；图片批量工作台的可选并发和旧工作区配置统一限制为 1–2 路。
+ [修复] 服务端校验生成结果的真实图片签名，损坏字节不再被保存为成功图片；内容审核错误继续原样返回。
+ [测试] 新增信号量峰值、排队取消、结果顺序、入口并发门禁、损坏结果与 `imagine:content-moderated` 真实原因回归。

## v3.0.39 - 2026-07-31

+ [修复] 画布普通批量生图最多同时提交 2 个任务，避免多张参考图编辑重复上传大文件时阻塞请求并触发网关 524。
+ [修复] 图片任务提交遇到 408、5xx 或网络中断时，先按原任务 ID 查询服务端是否已接收；已创建的任务继续等待并回填结果，不再把中转站已完成的任务误判为失败。
+ [测试] 图片任务恢复回归新增批量并发上限与模糊提交恢复门禁。

## v3.0.38 - 2026-07-30

+ [修复] “不同风格 / 换个风格”等模糊参考图编辑在单张模式生成一张相对原图明显不同的完整图片；多张模式为每个独立 `n=1` 任务分配不同风格方向。
+ [约束] 生成数量只控制独立图片节点和请求数量，不改变单张图片版式；每个风格结果继续禁止宫格、拼图、分屏和多版本合图。
+ [测试] 新增模糊风格识别、单张完整画面、四张独立风格编号及明确“黑白风格”不被覆盖回归。

## v3.0.37 - 2026-07-30

+ [修复] 普通参考图编辑默认锁定为一张连续完整画面；“不同风格”等短指令不再被模型解释成单图内多版本宫格。
+ [兼容] 仅当用户明确写出宫格、拼图、分屏、对比图、多面板、联系表或 2x2/3x3 等布局要求时允许多格输出，不影响 12 宫格分镜专用链路。
+ [测试] 新增普通风格编辑单画面锁与显式宫格放行回归。

## v3.0.36 - 2026-07-30

+ [修复] 画布重试会按段去重提示词；已解析的上游文本不再被重复追加，历史上已累加多遍的失败节点在下次重试时自动收敛为单份提示词。
+ [修复] 顶部“一键重试”只提交最下游失败节点，不再同时重试配置父节点、批次根节点和图片子节点，避免重复任务与额外扣费。
+ [修复] 裸露、色情、情色、内容政策及防护限制等上游审核原因统一识别为“内容审核未通过”，画布和生图工作台明确说明并非服务器故障，同时保留上游真实原因。
+ [测试] 新增提示词去重、失败叶节点筛选和中英文内容审核识别回归；图片任务恢复、Google 图片契约、TypeScript 与生产构建检查通过。

## v3.0.35 - 2026-07-24

+ [修复] 图片实时提交不再被“刷新后恢复”逻辑误接管；避免任务尚未创建就被查询为“图片任务不存在或已过期”，并取消真正的 GPT Image 2 请求。
+ [测试] 图片任务回归新增实时提交隔离门禁，并通过专项测试、TypeScript 与生产构建检查。

## v3.0.34 - 2026-07-23

+ [修复] 画布生图改为服务器持久任务：中转站完成后结果会暂存 48 小时，页面刷新、离开后返回或短暂断网时自动恢复到原图片节点。
+ [修复] 每个图片节点保存唯一任务 ID；重复查询或恢复不会再次向中转站提交，避免结果已成功却显示“任务已中断”和重复扣费。
+ [修复] 画布“停止生成”同步取消服务器上游请求；生成期间离开页面会提示确认。
+ [测试] 增加后台完成、结果读取、同任务幂等及主动取消回归，并通过 Google 1K/2K/4K 契约、TypeScript 与生产构建检查。

## v3.0.33 - 2026-07-23

+ [修复] GPT Image 2 尺寸归一化同时遵守 3840px 最长边和 8,294,400 总像素上限，旧画布中的 3840×2576 失败节点重试时会自动收缩到安全尺寸。
+ [修复] GPT Image 2 的 4K 预设和尺寸标签使用模型自身像素预算，不影响 Google 4K 及其他生图模型。
+ [测试] 增加 3:2、1:1、4:3 超限尺寸、16 像素对齐和旧画布自动修复回归。

## v3.0.32 - 2026-07-22

+ [修复] 生成配置节点的图像规格面板现在始终跟随节点已选模型；GPT Image 2 不再被全局 Google 4K 模型覆盖分辨率和比例选择。

## v3.0.31 - 2026-07-22

+ [修复] 画布节点切换为 GPT Image 2 时，不再继承 Google 原生 4K 的超限像素尺寸；保留宽高比并自动收缩到可用范围。
+ [修复] 节点图像设置现在以节点已选模型为准，不再错用全局 Google 模型判定分辨率和比例。
+ [兼容] 请求层会自动修复旧画布中已保存的超限尺寸，生图工作台、图片批量和默认模型切换同步生效。
+ [测试] 增加 Google 4K 切换 GPT Image 2、超宽比降级和合法尺寸保留回归。

## v3.0.30 - 2026-07-21

+ [修复] 画布 Agent 在执行 22 个工具前按既有 JSON Schema 递归校验参数；未知字段会剥离、留痕并继续执行，缺少必填字段则安全失败。
+ [埋点] `agent_turn.toolCalls[].errorKind` 新增 `unknown_keys_stripped` 与 `missing_required`，区分参数兼容清洗和真实缺参失败。
+ [测试] 增加工具参数白名单、嵌套数组、类型/枚举、必填字段和原型污染防护回归。

## v3.0.29 - 2026-07-21

+ [修复] 画布 Agent 对参考图产品的比例、饱和度与多边形边数改用相对锚定，不再把不可靠的绝对视觉判断写入生成提示词；保留颜色类别、材质质感、部件关系与不超过 5 个的清晰特征计数。

## v3.0.28 - 2026-07-21

+ [修复] `prompt_edited` 与生成链路共用同一节点引用解析结果，`beforeText` / `afterText` 均记录最终文本；上游引用缺失、断连或内容为空时保守丢弃整条编辑事件。

## v3.0.27 - 2026-07-21

+ [新增] 画布 Agent、生成结果、提示词人工改写、节点重生成/删除/下载/下游连接及融图确认行为支持匿名批量埋点；仅上报白名单文本和行为元数据，疑似图片载荷或图片 URL 整条拒收。
+ [新增] 埋点接收端按日追加 JSONL，支持一键关闭、请求/批次/单事件限额与 Docker 宿主持久化；新增只读 `telemetry-report.mjs` 汇总能力使用、工具失败、重生成、人工改写和 Agent 空转情况。

## v3.0.26 - 2026-07-21

+ [新增] 我的素材支持场景、人物、男模、女模等直接可见的预设分类和自定义分类；从画布、生图、视频及提示词库保存时会先弹出分类菜单。
+ [新增] 顶部导航新增“图片批量”工作台，支持多图拖拽上传、同一提示词逐张独立处理、1–5 路并发、单张超时、停止、逐项状态与重试。
+ [新增] 图片批量结果支持单张下载、ZIP 打包下载，以及通过分类菜单保存到“我的素材”；当前批次和结果会保存在浏览器工作区。

## v3.0.25 - 2026-07-19

+ [新增] 渠道兼容Gemini格式。
+ [修复] 12 宫格生成独立关键帧时先按 Beat 切出对应单格，宫格锁定构图、服装和动作，原始素材只锁定人物与商品身份。
+ [修复] 整片视频优先直接使用已批准的独立关键帧，不再因身份参考图二次重画人物；旧版错误桥接首帧自动失效。
+ [修复] 删除白色罩衫、桌子、独立衣物和替换口播等品类特判，恢复宫格中的原人物、原服装、原商品、Beat 顺序与原始台词。
+ [优化] Grok 整片提示词改为干净首帧、视觉身份、时间顺序和禁止新增项四级约束，原口播严格只说一次，连同商品尺寸锁保持在 180 词以内。
+ [测试] 新增宫格单格权威、关键帧直送、旧桥接失效、服装/商品/口播不改写及提示词长度回归。

## v3.0.24 - 2026-07-18

+ [修复] 真人带货整片恢复 v3.0.15 实测成功的“最多 4 个稳定镜头 + 硬切”结构，不再强迫人物 15 秒全程正脸边做复杂动作边念稿。
+ [优化] 口播恢复为 6/10/15 秒对应 10-14 / 18-24 / 26-34 个简单英文词，保留口语缩写和开场短句可见口型，演示段继续同一声线画外音。
+ [修复] 恢复中转站渠道 32 的 HTTPS/443 内部路由，避免 NewAPI 重启后将成功视频的 18011 内网下载误判为 SSRF 并让画布报失败。
+ [测试] 增加稳定分镜、开场口型转画外音、口语缩写、旧短稿补全与 180 词供应商上限回归。

## v3.0.23 - 2026-07-18

+ [修复] 真人带货口播恢复短开场可见口型加同声线前景画外音，不再强迫人物在整个演示过程中持续对口型。
+ [优化] 台词从逐字背诵改为自然表达同一意思，保留英文口语缩写、半秒反应、轻微停顿和重音变化，并提高人声在环境音中的前景感。
+ [测试] 增加自然 UGC 节奏、缩写保留、开场口型、画外音衔接及旧僵硬指令清除回归。

## v3.0.22 - 2026-07-18

+ [修复] 真人带货整片在保持单一连续镜头和干净首帧的同时，恢复反应、演示、展示和 CTA 的连续动作密度；删除小幅手势、固定构图、禁止弯腰等会将表演锁死的过量约束。
+ [优化] 6/10/15 秒真人口播调整为 8-11 / 15-19 / 23-28 个简单英文词，覆盖视频大部分时长；改用紧凑、有活力的创作者节奏，不再强制放慢和逐词过度发音。
+ [修复] v3.0.21 已保存的短口播可从 beats[].spokenLine 在本地重建为完整长度，无需再调用提示词优化模型。
+ [测试] 增加旧短口播升级、连续三动作、活力声线、非呆滞约束和纯商品多镜头分流回归。

## v3.0.21 - 2026-07-18

+ [修复] 真人口播整片改为单一连续创作者镜头，不再将多个分镜动作压入同一次 Grok 生成，避免叠化重影、人物换姿和肢体变形；纯商品或画外音视频仍保留多镜头分镜。
+ [优化] 口播开始前留出短暂安静间隔，模型输入前自动展开英文缩写并去掉无意义填充词，强制放松语速、逐词发音和全句可见口型同步。
+ [测试] 增加真人连续镜头与纯商品多镜头两条编译回归门禁。

## v3.0.20 - 2026-07-18

+ [修复] 刷新后的整片视频可从节点自身保存的 `commerceVideoPlan` 与干净锚点模式恢复分镜身份，不再依赖直接连接的 12 宫格；多代连续生成均保持 Grok 单图 I2V 路径。

## v3.0.19 - 2026-07-18

+ [修复] 整片分镜视频从上一条成片继续生成时，不再把上游视频或音频误传给仅支持干净首帧的 Grok Fast；刷新页面后仍可复用已保存的图片锚点连续生成。

## v3.0.18 - 2026-07-18

+ [优化] Grok 整片口播明确要求放松语速、逐词清晰且不含混，改善短句开头的吞字和连读；尺寸锁合并后总提示词仍不超过 180 词。

## v3.0.17 - 2026-07-18

+ [修复] 旧分镜已有长口播时先在本地按 6/10/15 秒稳定压缩，不再白等优化模型重写后才发现字数不合格。
+ [修复] `gpt-5.6-sol` 恢复结果不符合时长时增加确定性口播校正，避免直接中止整个视频链路。
+ [修复] 旧分镜恢复提示词的字数标准与 Grok 编译器对齐为 7-10 / 12-16 / 18-22 词，清除残留的过长口播规则。

## v3.0.16 - 2026-07-18

+ [修复] 线上视频任务发现的二次截断：声线不再停在 `with a brief`，分镜动作不再停在 `revealing / removed black`，保留完整表演风格、商品和服装宾语。
+ [优化] 为每个整片阶段保留最多 12 个动作词，同时压缩重复锁定语，供应商最终提示词仍不超过 180 词。
+ [优化] Grok 真人口播改用留呼吸的短稿档位：6 秒 7-10 词、10 秒 12-16 词、15 秒 18-22 词；旧长稿会先用 `gpt-5.6-sol` 重写，避免后半段变成含混不清的伪单词。

## v3.0.15 - 2026-07-18

+ [修复] 视频节点编辑面板中重新生成时，即使提示词含 `CommerceVideoPlan`，也必须提交视频任务；不再错误转去并发生成 3 张 12 宫格图。
+ [测试] 增加视频节点与分镜规划分流回归门禁。

## v3.0.14 - 2026-07-18

+ [修复] 成功的分镜整片从编辑面板再次生成时，复用已保存的 `generated-bridge` 或独立关键帧，不再重复调用图片模型、重画人物或多扣一次首帧费用。

## v3.0.13 - 2026-07-18

+ [修复] 真实旧分镜恢复测试发现的动作残句：整片编译器优先使用 `eightElements.action`，不再把“Following the panels...”类宫格顺序说明硬截成 `a / the / the model` 等无意义片段。
+ [优化] Grok 整片提示词连同尺寸锁限制在 180 词以内，保留完整口播与四段可执行动作；明确禁止叠化、交叉淡化和幽灵重影，首句口型后立即用同声续讲。
+ [实测] 线上历史 12 宫格完成语义恢复、干净首帧重建和 Grok Fast 整片生成；画布成功回填 15.04 秒 720×1280 H.264 + AAC 双声道视频。

## v3.0.12 - 2026-07-18

+ [修复] 12 宫格整片不再硬编码截取第 5 格作为视频首帧，避免非等高宫格产生分隔线、泡沫特写开场和人物拉伸。
+ [修复] 整片视频先用独立关键帧、原始人物/商品参考和完整宫格重建一张干净高清首帧，再作为 Grok I2V 的唯一视觉锚点；身份、产品结构与尺寸均由原始素材锁定。
+ [修复] 旧视频节点重试时拒绝复用历史宫格切片，重建成功后立即保存首帧参考，网络中断后可直接续试而不再重画。
+ [优化] 分镜视频使用已编译的精简 Grok 指令，保留完整自然口播、可读嘴型、硬切镜头和人物/商品一致性，不再叠加通用长模板产生互相冲突的指令。
+ [测试] 新增干净首帧、紧凑提示词、尺寸锁和旧任务重试的回归覆盖。

## v3.0.11 - 2026-07-17

+ [修复] 视频任务轮询遇到画布部署重启、短暂 502 或网络闪断时继续查询原任务，不再提前把仍在生成的视频标记为超时，也不会重复提交或扣费。
+ [修复] Grok 视频完成后通过带鉴权的画布代理下载 MP4，不再把中转站受保护的内容地址直接交给视频节点。
+ [恢复] 找回任务 `task_8iCQHxcGPlFG14MzmgAKvrqRcOgG5bUu` 的 15 秒 720p 成品，确认视频与 AAC 音轨完整。

## v3.0.10 - 2026-07-17

+ [新增] Google `gemini-3.1-flash-image` 拆分为 1K、2K、4K 三个公开模型名，画布选档、中转站日志和计费档位保持一致。
+ [优化] 三档模型统一映射官方上游模型，传递原生分辨率与 14 种官方比例，直接保留上游图片，不再浏览器拉伸或锐化。
+ [迁移] 历史无后缀模型按已保存尺寸自动迁移到对应档位，不开放 0.5K。
+ [测试] 新增 Google 图片模型契约回归测试，覆盖模型名、分辨率、比例及代理前缀。

## v3.0.9 - 2026-07-17

+ [修复] 视频创作台恢复独立的 `gpt-5.6-sol` 智能编导，真人带货默认生成精简镜头和完整自然口播，自由创作保留用户的声音意图。
+ [修复] 创作台在智能编导和视频提交之间共用同一份已实体化参考图，不再产生 `image: {}` 或虚假长时间运行；历史文件丢失时立即显示真实原因。
+ [优化] Grok Fast、Preview 和 1080p 全部保留真人带货能力，按模型正确选择 i2v / r2v 参考模式，无禁用、降级或“不适合真人”警告。
+ [优化] 口播提示词锁定同一主播、开场清晰口型、同声线画外音和商品/人体比例；截断时退回上一个完整句子，避免残缺指令和超长提示词。
+ [测试] 生产环境完成 Fast、Preview、1080p 三模型 6 秒真人带货样片：三条均含 AAC 双声道，口播逐字与指定台词一致，首帧、人脸、手指和项链保持稳定。

## v3.0.8 - 2026-07-17

+ [修复] 融图 planner 忽略上游 `data: ping` 等非 JSON SSE 心跳，不再误判整轮规划失败。
+ [工程] 移除 Next.js `ignoreBuildErrors`，生产构建恢复强制 TypeScript 类型门禁。
+ [测试] 新增真实 1 场景 + 3 产品融图 planner 流式 smoke 工具，记录首数据块与总耗时并校验摆放计划完整性。

## v3.0.7 - 2026-07-16

+ [修复] 融图场景规划恢复流式视觉分析，避免长请求经过 Cloudflare 时发生 524；同时兼容标准 SSE 与非流式 JSON 返回。
+ [优化] planner 专用参考图在浏览器端缩至最长边 512px，最终融图仍使用原始图片，降低视觉分析等待时间与输入成本。
+ [修复] 清理提示词封面代理的 TypeScript `BodyInit` 类型错误，`tsc --noEmit` 恢复通过。
+ [修复] 节点悬浮工具栏适配明暗主题，右键菜单完成中文化，并补齐视频、导演台悬停提示及撤销、重做、全选、取消选择快捷键说明。

## v3.0.6 - 2026-07-14

+ [修复] 12 宫格整片使用原始审阅图中的干净人物面板作为 Grok Fast 首帧，不再发送宫格，也不再经过会改变脸型、身体和服装的生图重绘桥接。
+ [修复] 整片提示词在长度限制内保留四段完整动作语义，避免末段被截断后由模型自行插入商品特写、人物重影或错误转场。
+ [优化] 6 秒、10 秒和 15 秒分别使用独立口播稿；默认采用短正面口播加同声线旁白，减少台词压缩、跨镜头假口型和人物漂移。
+ [优化] 恢复经过实测的 Grok Fast 单图 I2V 包装约束，并将内置提示词润色模型统一为 `gpt-5.6-sol`。
+ [测试] 线上任务 445 完成真实画布端到端验收：15.04 秒、首帧单人、口播逐字一致、嘴型可见、四段动作完整、无双人重影，人物和服装保持一致。

## v3.0.5 - 2026-07-14

+ [修复] 分镜整片和产品锁定视频的高清首帧重建不再静默卡住 10 分钟；主模型超时或网络故障时，120 秒内终止并切换独立备用生图模型。
+ [优化] 视频节点明确显示“重建高清首帧”、“切换备用模型”和“视频任务提交/生成中”三个阶段，避免将前置生图误认为 Grok 视频请求。
+ [安全] 备用路径仅处理超时、连接中断和 5xx 类故障，不对内容安全、权限或参数错误进行绕过。
+ [测试] 分镜、Grok 模型、视频代理与融合回归全部通过，生产构建通过。

## v3.0.4 - 2026-07-13

+ [修复] 12 宫格整片不再把宫格或低清裁片直接作为 Grok Fast 的真实首帧；优先使用已生成的干净关键帧，否则先将开场面板重建为高清桥接首帧，消除开场宫格融合、人物弯曲和肢体重影。
+ [修复] 整片重试仅复用明确保存的关键帧或桥接首帧；旧节点自动升级到新首帧流程，不再重新带入历史宫格或变形裁片。
+ [优化] 口播窗口复用同一正面人物机位并锁定脸型、发型、镜头与光线；中段口播结束后才允许无脸 B-roll，中文短句限制长度，减少台词溢出、假口型和跨镜头身份漂移。
+ [优化] 强化硬切与人体结构约束，禁止人物交叠、克隆、溶解、拉伸、肢体融合和过渡残影。
+ [测试] 分镜、Grok 三模型与融合提示词回归及生产构建通过；线上 15.04 秒 Fast I2V 样片完成首帧、密集帧、逐字转写、人物结构、节点保存和播放验收。

## v3.0.3 - 2026-07-13

+ [修复] 12 宫格整片生成恢复 Grok Fast 15 秒单图 I2V，生成与重试都携带同一张干净人物/商品身份锚图，不再退化为无参考图 T2V。
+ [修复] 旧分镜恢复 Grok 原生镜头内口播，由模型围绕可见人物表演自然组织短句，不再强塞覆盖全片的逐秒旁白。
+ [修复] 新分镜仅在 `spokenLine` 存在的正脸镜头中生成台词；细节、背面、转身和无脸 B-roll 强制静音，防止冻结嘴型和画外音假说话。
+ [优化] 同一台词期间锁定同一成年主播、清晰正脸和连续唇、下颌、面部与呼吸动作；强化人物、服装、声线和多场景连续性。
+ [优化] 新建 apparel / subject 分镜默认使用 3-5 个语义相关地点的生活方式蒙太奇，内置提示词润色模型统一为 `gpt-5.6-sol`。
+ [测试] Fast、Preview、1080p 模型契约与分镜提示词回归全部通过；线上 15.04 秒 I2V 样片完成逐字转写、连续嘴型帧、人物一致性、节点保存与播放验收，控制台无异常。

## v3.0.2 - 2026-07-13

+ [修复] 12 宫格整片生成恢复 15 秒 Grok T2V 请求，不再把宫格作为 I2V 参考而被限制为 10 秒；整片重试保持同一请求模式。
+ [优化] 旧 3.0 分镜缺少口播稿时使用四段自然中文销售口播，将展示与 CTA 合并收尾，避免朗读导演说明、第五句丢失和提前结束。
+ [优化] Grok 整片提示词优先保留完整事件链、地点顺序和人物/商品身份约束，并移除未实际发送的图片引用。
+ [修复] 图片与分镜节点生成视频时使用当前视频时长，不再继承图片节点中的过期时长元数据。
+ [测试] 新增整片 T2V、口播时间缩放、旧分镜模式识别、提示词长度和无虚假参考图回归；线上完成主备通道 A/B 与逐句转写验收。

## v3.0.1 - 2026-07-13

+ [修复] 整片和分段 Grok 视频恢复可听口播，新分镜保存声线、语言、整片口播稿和逐镜台词，旧 3.0 分镜自动使用口播兼容规则。
+ [修复] 补齐 Grok Fast、Preview 和 1080p 的参考图数量、时长、请求模式与固定分辨率约束，不再静默丢弃超额的用户参考图。
+ [测试] 新增三种 Grok 视频模型契约回归，扩展分镜口播和旧数据兼容回归。

## v3.0.0 - 2026-07-12

+ [新增] 电商视频 3.0 工作流：分镜规划、12 宫格审阅、干净关键帧、分段视频与整片视频。
+ [新增] 导演简报和场景顺序约束，支持多场景生活方式蒙太奇并保持人物、服装和产品身份一致。
+ [优化] Grok 视频提示词按镜头编译，整片生成固定场景顺序、硬切规则和安全参考图。
+ [优化] 视频时长按分镜数量自动匹配，Grok 模型设置与参考图限制保持一致。
+ [优化] 内置提示词润色模型升级为 `gpt-5.6-sol`，并迁移旧的默认模型配置。
+ [修复] 防止 12 宫格审阅图直接进入视频、防止伪造商品与错误产品比例，并补强合规约束。

## v0.4.0 - 2026-06-16

+ [新增] 新增网页版Agent Loop模式。
+ [新增] 支持Vercel一键部署。
+ [调整] 移除后端，项目定位为个人画布工具。

## v0.3.0 - 2026-06-15

+ [新增] 新增canvas-agent通过codex操作画布。

## v0.2.5 - 2026-06-08

+ [新增] 新增图片切图功能。
+ [新增] 支持webdav同步数据。
+ [修复] 修复画布文字节点错误问题。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含视觉画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
