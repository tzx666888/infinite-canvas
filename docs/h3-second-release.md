# H3 按秒计费 v3.157.42

## 范围与价格

仅 H3：10 秒原价除以 10 为每秒积分。720p（两种名称）2.5、2K 3、1080p 3.5、1080p Pro 4.5。15 秒线性计算。Seedance、Omni、SD30 不变，不增删模型。

报价读取本账号真正生效的分销价，与预扣共用计算。失败退款保留原始金额，不用新单价重算旧任务。旧 H3 分销按次规则除以 10 解释，后台编辑后按秒保存。

## 发布

1. `scripts/h3-second-price-migration.py prepare` 备份现网 compose、SQLite 和 H3 规则，生成新配置；密钥只留在服务器私有备份中。
2. 提交版本、推送 main / v3.157.42，构建镜像并完成无费用回归、镜像与报价验证。
3. `apply` 只处理原先因单位不符而被忽略的 H3 分销规则，维持其原有效底价，防止意外启用旧高价；活跃按次规则由新代码转换，不改客户余额、任务或历史账本。
4. 新旧实例并行验证、Nginx 平滑切换，保留旧镜像。正式 compose 使用生成的 after 文件。

## 回滚

镜像 `ghcr.io/tzx666888/infinite-canvas:v3.157.41`；恢复备份 compose 后重建，再执行脚本 `rollback`。规则有并发管理员编辑时停止并人工核对，不能覆盖。不要用整库备份覆盖运行中的客户新订单或余额。

## 验收

本地临时数据库测 H3 全档位报价=扣费、10/15 秒、JSON/multipart、重复请求、单次退款和其他模型不变。线上仅只读报价与页面检查，不提交上游付费视频。审阅 todo：无相关待办需要删除。
