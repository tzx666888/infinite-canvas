# v3.157.44 H3 时长滑块与画布模型范围

v3.157.43 未上线，发布修正保留既有文本/图片默认选择，只过滤视频选项。

- 现有 metaso/xhaay H3 v0.2.0 桥接已接受 4–15 秒整数，1080p 编排转发时长；本次只修改画布，不改共享渠道。
- 滑块步长 1 秒；请求规范化、预检、Agent 能力范围、报价和预扣接受相同秒数。原每秒价、分销价和历史账本不变。
- 隐藏画布官方/旧 Seedance 视频型号：默认列表、同步接口、本地配置加载统一过滤。保留服务端提交/轮询能力，避免影响已经开始的任务；豆包文本不隐藏。
- 依用户要求不另行执行测试或提交视频生成。发布只进行构建与必要的版本/健康确认；既有 GitHub 发布工作流保持不变。
- 备份：服务器 `/opt/backups/canvas-v3.157.43-h3-slider`，包含 compose、Nginx 与在线 SQLite 快照。回滚镜像 `ghcr.io/tzx666888/infinite-canvas:v3.157.42`；只回滚代码/容器，不恢复整库覆盖新账单。
- 已检查 todo，无相关待办删除；新变更在 pending-test 中记录，实际生成效果不在本次验证范围。
